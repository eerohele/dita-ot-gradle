language: groovy
before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
before_script:
  - git submodule update --init --recursive
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
